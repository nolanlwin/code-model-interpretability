schema: forall
context: |
  This project uses Forall vericoding. Write specs before code.
  Run `forall check` before archive.
rules:
  verification:
    - Run `forall check --change <name>` and fix all CRITICAL issues before archive
