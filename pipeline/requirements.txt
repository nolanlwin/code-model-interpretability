# Minimal dependencies for the unified pipeline (tested versions in comments)
torch>=2.1            # 2.10.0
transformers>=4.40    # 5.3.0
scikit-learn>=1.3     # 1.7.2
numpy>=1.24           # 2.2.6
tqdm>=4.64            # 4.67.3
datasets>=2.18        # 4.8.4  (only for loading from the HF Hub)
huggingface_hub>=0.23 # 1.7.1  (only for pipeline.hf_upload)
